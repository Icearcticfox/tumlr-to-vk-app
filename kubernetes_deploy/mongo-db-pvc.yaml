apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-mongo-pvc
  namespace: applications
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi